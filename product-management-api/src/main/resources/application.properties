spring.application.name=product-management-api

spring.config.import=optional:file:.env
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Database
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/product_management}
spring.datasource.username=${DB_USERNAME:Javauser}
spring.datasource.password=${DB_PASSWORD:Soumik$4321@}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=8080

# CORS
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:8080}

# JWT Configuration
jwt.secret=${JWT_SECRET:Stl8mM0qXk1MCXnwSP4W6GEuCPuvLUutu7E2GP0WeXk=}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Redis Configuration
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}