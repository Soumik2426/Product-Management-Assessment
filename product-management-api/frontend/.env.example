# Use mock localStorage data (default). Set to false to call the Spring Boot API.
VITE_USE_MOCK_API=true

# REST API base URL. Leave empty in dev to use the Vite proxy (/api -> localhost:8080).
# VITE_API_BASE_URL=http://localhost:8080
